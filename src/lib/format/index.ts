/**
 * 展示层格式化工具的统一入口。
 *
 * 只聚合「纯展示、无领域耦合」的格式化模块：`title`（浏览器标签标题）、
 * `date`（日期显示）。带领域不变量的格式化不属于这里——例如 `formatYuan`
 * 是「分→元」展示边界的一部分，与金额以整数分流转的领域不变量同属
 * `$lib/domain/money`，UI 直接从那里 import 更诚实，也避免本入口对领域层
 * 做局部转发（只透出某几个导出会很别扭）。
 */
export * from './title';
export * from './date';
